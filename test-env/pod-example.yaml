apiVersion: v1
kind: Pod
metadata:
  generateName: test-pod-
  labels:
    role: myrole
spec:
  containers:
    - name: web
      image: nginx
      ports:
        - name: web
          containerPort: 80
          protocol: TCP

